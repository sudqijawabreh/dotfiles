#!/bin/bash

curl -sS -X 'POST' \
  'http://10.10.20.4/Setting' \
  -H 'accept: application/json' \
  -H "X-R365-Customer-instance: ${1}" \
  -H 'Content-Type: application/json' \
  -d '[ 1,2,3,4,5,6 ]' | jq 'map({(.settingId): .value}) | add'

